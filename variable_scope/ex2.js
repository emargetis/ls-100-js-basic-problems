console.log(greeting);

let greeting = 'Hello world!';

/*
It will throw an error because the greeting isn't
declared and initialized using let until after the 
console.log function is passed 'Hello world!' as an 
argument.
*/